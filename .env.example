# Crew · 环境变量示例（复制成 .env 并填入真实值）
#
# 首次跑 crew.exe 会弹向导让你填 API key，不需要提前建这个文件。
# 只有想手动配置或 headless 部署时才用得着。

# 通用 LLM key（推荐用这个，provider 通过 CONFIG 或向导选）
LLM_API_KEY=

# 旧兼容变量（v1.0 用 ARK 的老用户可以留）
ARK_API_KEY=
